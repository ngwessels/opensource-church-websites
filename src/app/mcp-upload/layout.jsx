export default function McpUploadLayout({ children }) {
  return (
    <div className="flex min-h-full flex-1 items-center justify-center bg-gradient-to-br from-background via-muted to-accent/30">
      {children}
    </div>
  );
}
