/** Max file size for media library uploads (sourceUrl and browser upload links). */
export const MAX_MEDIA_UPLOAD_BYTES = 10 * 1024 * 1024;

/** How long an unused upload link stays valid. */
export const UPLOAD_LINK_TTL_MS = 60 * 60 * 1000;

export const ALLOWED_FOLDER_IDS = ["pictures-root", "documents-root", "unused-pictures"];
